# Internal implementations
It's C, so these can be accessed from outside of the library, but most of the
functionality within here is completely useless without being inside the lib.

All internal implementation functions and structs start with `lc_impl`.